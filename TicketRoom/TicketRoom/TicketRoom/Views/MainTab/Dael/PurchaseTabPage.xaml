<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:custom="clr-namespace:TicketRoom.Models.Custom"
             x:Class="TicketRoom.Views.MainTab.Dael.PurchaseTabPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="1"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!--<Grid Grid.Row="0"  x:Name="SubTabGrid"/>-->
        <ScrollView Grid.Row="0" x:Name="TabScoll" Orientation="Horizontal"/>
        <BoxView Grid.Row="1" BackgroundColor="LightGray" Opacity="0.5"/>
        <!--#region 전체 화면 그리드(행) - 0 컨텐츠 1 탭-->
        <ScrollView Grid.Row="2" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
            <Grid x:Name="Purchaselist_Grid" BackgroundColor="White" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand" RowSpacing="0" ColumnSpacing="0"/>
        </ScrollView>
        <!--#endregion-->
    </Grid>
    
</ContentView>